node_modules/
**/node_modules/

.env
**/.env
!.env.example

api/uploads/*
!api/uploads/.gitkeep

dist/
**/dist/

logs/
**/logs/
*.log

# Local-only MySQL 8.4 data dir (see AGENTS.md "Database Lokal") — never
# commit actual database files.
.mysql-data/

# Local AI coding-assistant workspace (dev-server launch config, worktrees,
# task locks). Machine-specific tooling, not part of the app.
.claude/

# Dump database lokal sebelum operasi berisiko — jangan ikut ke repo.
backups/

# Folder sementara scripts/siapkan-produksi.js
api/prisma/.migrasi-ditunda/

# Tautan project Vercel CLI (lokal per mesin)
.vercel/
